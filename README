This repository will contain the REST api service layer and the mid-tier logic for serving front end requests.
It will communicate with the front-end via rest calls and with the back-end service through asynchronous messaging.
Right now I plan to code this layer in Python.